---
name: PROJECT-SLUG
plan_version: 1
status: planning
created: YYYY-MM-DD
updated: YYYY-MM-DD
mode: greenfield
product_form: PRODUCT-FORM
archetype: ARCHETYPE
archetype_confidence: ARCHETYPE-CONFIDENCE
overlays: []
public_release: PUBLIC-RELEASE-BOOLEAN
source_revision: SOURCE-REVISION-OR-NONE
input_digest: sha256:INPUT-DIGEST
validated_at: YYYY-MM-DDTHH:MM:SSZ
domains_applicable: []
domains_deferred: []
domains_excluded: []
progress:
  phases_total: 0
  phases_done: 0
  tasks_total: 0
  tasks_done: 0
---

# PROJECT-NAME master plan

One paragraph: what this project is, for whom, and the observable definition
of done ("done means: a new user can X, Y is deployed at Z, the suite passes").

## Scope and non-goals

In scope: the smallest first cut that proves the approach.
Non-goals: named exclusions with a word on why (deferred, out of scope, or rejected).
Scale and capacity: CALIBRATION; CAPACITY; at most N phases and N tasks; planned task appetites total N.

## Plan provenance

Source revision: SOURCE-REVISION-OR-NONE
Input digest: sha256:INPUT-DIGEST
Validated at: YYYY-MM-DDTHH:MM:SSZ
Evidence inventory:
- `intake` = `sha256:INTAKE-DIGEST`
- [recheck] `path/to/stable-artifact` = `sha256:EVIDENCE-DIGEST`

Inventory labels are unique, exactly one is `intake`, and every digest covers the named normalized intake text or raw file bytes. Mark completed or imported evidence that must be rechecked on resume in its label or an adjacent plan section without changing the machine-readable inventory line.

## Product form

Primary: web application | API or service | CLI or SDK | mobile or desktop | data or ML | infrastructure or IaC.
Vertical slice: the form-specific end-to-end path from discovery.md.
Completion evidence: the form-specific gate that proves a user-operable increment.
Secondary form: none, unless it has its own user, contract, distribution path, deliverable, and completion evidence.

### Archetype confidence

- Primary: ARCHETYPE (score 0.00)
- Runner-up: RUNNER-UP-ARCHETYPE (score 0.00)
- Margin: N points
- Confidence: high | medium | low
- Vetoes applied: none, or the archetype and the signal that zeroed it
- Overlays: none, or the frontmatter list
- If the runner-up is right: +N tasks and +N phases; what flips in the matrix and which GP tasks appear or drop

Margin is (primary - runner-up) * 100 rounded. Confidence is high at margin 15
or more and score 0.70 or more, medium with exactly one, low with neither; the
validator recomputes both. A low-confidence archetype also appears in Open
Questions, and no assure-stage documentation row may be not-applicable while it
stands.

## Compliance gate

Result: pass
Screened: YYYY-MM-DD against anthropic.com/legal/aup.
Account safety: unattended runs use supported workload authentication; no subscription credentials outside official clients.

## Applicability matrix

Statuses: applicable (pass runs now), deferred (deferrable set only: seo,
launch, observe, ui, deploy; the row names the trigger and the reversibility
argument), excluded (evidence state + project-specific reason + revisit when).

An excluded cell reads: `absent:` or `by-design:`, then the reason, then
`revisit when:` and an observable predicate. `unknown:` and `hint:` are refused.

| Domain | Status | Reason |
|---|---|---|
| product | applicable | |
| architecture | applicable | |
| stack | applicable | |
| database | applicable-or-excluded | state: reason; revisit when: predicate |
| security | applicable | never excluded or deferred, scales with calibration |
| llm | applicable-or-excluded | state: reason; revisit when: predicate |
| ux | applicable-or-excluded | state: reason; revisit when: predicate |
| ui | applicable-deferred-or-excluded | excluded: state + reason + revisit when; deferred: trigger + reversibility |
| seo | applicable-deferred-or-excluded | excluded: state + reason + revisit when; deferred: trigger + reversibility |
| code-quality | applicable | never excluded or deferred, scales with calibration |
| style-genome | applicable | |
| agent-memory | applicable | |
| repo | applicable | |
| build | applicable | |
| roadmap | applicable | |
| deploy | applicable-deferred-or-excluded | excluded: state + reason + revisit when; deferred: trigger + reversibility |
| observe | applicable-deferred-or-excluded | excluded: state + reason + revisit when; deferred: trigger + reversibility |
| launch | applicable-deferred-or-excluded | excluded: state + reason + revisit when; deferred: trigger + reversibility |

### Module disposition

One line per applicable module. `dropped-by` names the layer that removed a
requirement: scale, archetype, or form. A dropped ID appears on no task.

- MODULE: landed R-XXX-n, R-XXX-n; dropped-by scale R-XXX-n (reason)

Scale calibration: weekend | side-project | funded-product | enterprise.

## Decisions

Hard-to-reverse bets first. Each entry: the decision with rationale and rejected
alternatives, a structured Falsifier block (the observable signal, failure
boundary, and return-to-planning action), or a hypothesis with its validation
task, or a pointer to Open Questions.

### D1: (data-model shape / tenancy)
Falsifier:
- Signal: (metric or event and its evidence source)
- Failure boundary: (numeric threshold or observable event that kills D1)
- Replan action: return to planning and (reconsidered alternative or migration)
### D2: (auth and ownership boundary)
Falsifier:
- Signal: (metric or event and its evidence source)
- Failure boundary: (numeric threshold or observable event that kills D2)
- Replan action: return to planning and (reconsidered alternative or migration)
### D3: (wire format and public identifiers)
Falsifier:
- Signal: (metric or event and its evidence source)
- Failure boundary: (numeric threshold or observable event that kills D3)
- Replan action: return to planning and (reconsidered alternative or migration)
### Assumptions ledger
Defaults taken without asking, each flagged as a hypothesis, priced in tasks and
phases, and pointed at the task that validates it.

- A1: (the default taken, and what suggested it)
  - Blast radius: wrong costs +N tasks and +N phases; (what structurally changes)
  - Validated by: GP-nnn

## Requirements

### Story 1: (role) can (capability)
R-1.1: WHEN (trigger) THE SYSTEM SHALL (observable behavior).
R-1.2: IF (condition) WHEN (trigger) THE SYSTEM SHALL (observable behavior).

## Architecture

Prose plus the diagrams that carry decisions (mermaid: graph TD with trust
boundaries, erDiagram, sequenceDiagram for the load-bearing flow). No decorative
diagrams; each one sits next to the claim it supports.

## Style genome

Naming, file organization, error-handling idiom, comment policy, formatting
authority. Brownfield: extracted from the codebase, with file citations.

## Agent memory

The AGENTS.md and pillar files the scaffold task emits, listed with their
one-line purposes.

## Documentation set

This manifest covers documentation committed to this repository. Documents that
live in a wiki, an intranet, or a compliance platform are marked
present-elsewhere and are not planned here.

Catalog ids, stages, owners, and selection rules come from doc-set.md. Required
and recommended rows name the task that writes them. Not-applicable rows carry
an evidence state (absent or by-design), a reason, and a revisit-when predicate.

| Document | Stage | Verdict | Owner | Selected by, or reason and revisit when |
|---|---|---|---|---|
| decide.adr | decide | required | architecture | always; written by GP-nnn |
| build.readme | build | required | repo | always; written by GP-nnn |
| build.style-genome | build | required | style-genome | always; written by GP-nnn |
| assure.threat-model | assure | required-or-not-applicable | security | selecting signal and its task, or state + reason + revisit when |
| operate.runbook | operate | required-or-not-applicable | observe | selecting signal and its task, or state + reason + revisit when |
| verify.traceability | verify | not-applicable | roadmap | by-design: no external authorizer; revisit when: a contract or regulator requires an audit trail |

## Phases

## Phase 1: NAME

Goal: one sentence. Blocks: what this phase gates.

### Wave 1.1

- [ ] GP-101 [W1.1] Imperative title
  - Files: exact/paths.ext
  - Depends on: none
  - Reuses: what exists already
  - Acceptance: 2-4 grep-verifiable conditions
  - Verify: `exact command`
  - Requirements: R-1.1, R-DOM-n

Checkpoint: the independently observable outcome of Phase 1.
Checkpoint verify: `exact command that reproves the phase outcome`

Must-haves:
- Truth: observable behavior with its manual check
- Artifact: path that must exist with what it exports
- Link: proof the pieces are wired (import, route registration, config reference)

## Phase N: Verification

Goal: prove the built system satisfies the plan end to end.

### Wave N.1

- [ ] GP-N01 [WN.1] Full suite, lint, build, end-to-end smoke
  - Files: none (runs existing checks)
  - Depends on: PRIOR-TASK-IDS (replace with comma-separated GP IDs)
  - Reuses: the project's existing test, lint, build, and smoke commands
  - Acceptance: suite green, lint clean, build succeeds, smoke exercises the primary journey by its real command or manual path
  - Verify: `exact command chain, e.g. npm test && npm run lint && npm run build && npm run smoke`
  - Requirements: R-1.1, R-CODE-n, R-SEC-n (list the load-bearing IDs; never write "all")

Checkpoint: a fresh clone passes every check with one command sequence.
Checkpoint verify: `exact full-suite and smoke command`

## Open Questions

The only enumeration of open decisions, and only the residual ones: an unknown
that must be settled before dependent work is a flagged hypothesis in Decisions
with an R-ROAD-7 validation task scheduled ahead of it, not a question. Write
`None.` when there are none; otherwise one `### Q<n>:` entry each.

### Q1: (the question, phrased so its answer is a decision)
- Owner: (the role that answers; say when the answer is not the agent's to give)
- Blocks: build | ship | nothing, and the boundary it blocks at
- Decide by: (date, phase start, or wave boundary; never "later")
- Why it matters: (what changes on each answer)
- Options: (a) ...; (b) ...; (c) the option from outside the shared framing
- Recommended default: (the option the plan executes on if nobody answers, and why taking it early is safe)

## Rules for executing agents

> [!IMPORTANT]
> This plan is the source of truth. The chat is not.
>
> 1. Before any work: read the frontmatter and `## Plan provenance`, then re-derive state from disk. Recheck recorded completed or imported evidence; if material evidence drifted, change status to `planning`, record the stale evidence, and stop for replan. Otherwise refuse unless `status` is `approved` or `executing`. `planning` awaits sign-off; `done` is closed. Run `bash .godplans/validate-plan.sh .godplans/PLAN.mdx`. If status is `approved`, change it to `executing` and update the date before the first task.
> 2. Find the first unchecked task in wave order. Re-derive state from checkboxes; trust nothing remembered.
> 3. One task at a time. Respect Depends on. Tasks marked [P] may run concurrently; the validator has already proved their Files lists are disjoint from every other unchecked task in the wave, so treat [P] as the schedulable set and never widen it by hand.
> 4. Run the task's Verify command. Only after it passes, flip [ ] to [x] and update the frontmatter counters and `updated:` date in the same edit. Never batch check-offs. Regenerate the sidecar in the same batch: `bash .godplans/validate-plan.sh --allow-planning --emit-json .godplans/PLAN.json .godplans/PLAN.mdx`.
> 5. If Verify fails: the box stays unchecked. Append an indented `- Note (YYYY-MM-DD):` line under the task saying what happened.
> 6. Phase-boundary gate: before the first task of a new phase, run `bash .godplans/validate-plan.sh --drift-check N .godplans/PLAN.mdx`, replacing N with the completed phase. The command recomputes `[recheck]` provenance files, reruns a deterministic sample of up to three completed task Verify commands, and reruns the phase's Checkpoint verify command. Any failure returns status to `planning` and stops execution for replan.
> 7. If a deferred domain's trigger fires mid-execution (its named event occurs), return status to `planning` and run that domain's pass before continuing.
> 8. Scope changes are not improvised. Return status to `planning`, patch the plan (new task IDs, struck-through superseded tasks with a reason), increment `plan_version`, and obtain fresh approval before more execution.
> 9. Never renumber, reword, or uncheck a completed task.
> 10. At session end: append one line to `## Session log`: date, tasks completed, where you stopped, what is next.
> 11. Re-read the current phase before starting it; re-read Decisions before touching anything they govern.
> 12. After every task is checked and the final Verification phase passes, change status from `executing` to `done`, update the date, and run `bash .godplans/validate-plan.sh --allow-planning .godplans/PLAN.mdx` for a final structural check.

## Session log

- YYYY-MM-DD plan created (godplans v1.11.0)
